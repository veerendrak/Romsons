import { async, ComponentFixture, TestBed } from '@angular/core/testing';

import { ConfigGroupDetailsComponent } from './config-group-details.component';

describe('ConfigGroupDetailsComponent', () => {
  let component: ConfigGroupDetailsComponent;
  let fixture: ComponentFixture<ConfigGroupDetailsComponent>;

  beforeEach(async(() => {
    TestBed.configureTestingModule({
      declarations: [ ConfigGroupDetailsComponent ]
    })
    .compileComponents();
  }));

  beforeEach(() => {
    fixture = TestBed.createComponent(ConfigGroupDetailsComponent);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
