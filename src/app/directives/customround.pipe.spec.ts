import { CustomroundPipe } from './customround.pipe';

describe('CustomroundPipe', () => {
  it('create an instance', () => {
    const pipe = new CustomroundPipe();
    expect(pipe).toBeTruthy();
  });
});
